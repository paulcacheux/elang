extern fn print_int(int);
extern fn println_int(int);

fn main() -> int {
    let y = 0;
    while y <= 20 {
        let x = 0;
        while x <= 20 {
            print_int(x);
            println_int(y);
            x = x + 1;
        }
        y = y + 1;
    }
    return 0;
}
